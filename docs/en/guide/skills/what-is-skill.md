# What is a Skill?

A Skill is a reusable instruction template for AI assistants.

It defines:

- what to do (`name`, `description`)
- how to do it (`prompt`)
- what good output looks like (`examples`)
