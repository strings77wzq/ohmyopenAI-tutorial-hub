# 重试与熔断

## 概念

Agent 的每次工具调用都可能失败。重试策略决定了"失败后怎么办"；熔断器决定了"什么时候该停下"。这两个机制共同防止 Agent 在不可恢复的错误上浪费资源。

## 为什么需要错误分类？

不是所有错误都应该重试。把"API 限流"和"权限拒绝"用同一策略处理，会导致 Agent 在真正无解的问题上反复撞墙。

## 错误分类与路由

| 错误类型 | 典型触发场景 | 策略 | 最大重试 |
|----------|-------------|------|----------|
| **网络超时** (TimeoutError) | API 调用、文件下载 | 指数退避重试 | 3 次 |
| **API 限流** (RateLimitError) | 高频 API 调用 | 等待 Retry-After 头后重试 | 3 次 |
| **工具调用格式错误** | 参数类型不匹配 | 修正参数后重试 | 2 次 |
| **编译/构建错误** | 语法错误、类型错误 | 分析错误信息 → 修复 → 重试 | 3 次 |
| **模型幻觉** | 输出不符合 schema | 要求重新生成，加强约束 | 2 次 |
| **权限拒绝** (PermissionDenied) | 写保护目录、无权限 API | **立即停止，不重试** | 0 次 |
| **资源不存在** (NotFound) | 文件路径错误、API 端点不存在 | 验证路径后重试一次 | 1 次 |

关键原则：**可恢复错误可以重试，不可恢复错误必须立即停止**。区分标准是"重试会不会改变结果"——权限拒绝重试 100 次也还是拒绝。

## 指数退避（Exponential Backoff）

对于可恢复的瞬时错误，指数退避是最经典的策略：

```
Attempt 1: wait 1s   → fail (timeout)
Attempt 2: wait 2s   → fail (timeout)
Attempt 3: wait 4s   → fail (timeout)
Attempt 4: wait 8s   → fail → ESCALATE (超过最大重试次数)
```

### 退避实现要点

- **基数**：初始等待时间不宜太小（建议 ≥ 1s），避免对过载的服务雪上加霜
- **上限**：最大等待时间不应超过任务的总超时限制（建议 ≤ 30s）
- **抖动**：在退避时间中加入随机因子（±20%），避免多个 Agent 同时重试造成惊群效应

```
base_wait = min(initial * (2 ^ attempt), max_wait)
actual_wait = base_wait * (0.8 + random() * 0.4)  // ±20% jitter
```

## 熔断器（Circuit Breaker）

熔断器是防止级联失败的经典模式，在 Agent 场景下同样适用：

```
        ┌──────────┐
        │  CLOSED  │ ← 正常状态：请求正常通过
        └────┬─────┘
             │ 连续失败 ≥ N 次
             ▼
        ┌──────────┐
        │   OPEN   │ ← 熔断状态：立即拒绝所有请求
        └────┬─────┘
             │ 冷却时间过后
             ▼
        ┌──────────┐
        │ HALF-OPEN│ ← 试探状态：允许少量请求通过
        └────┬─────┘
      成功    │    失败
       ▼      │     ▼
    CLOSED    │   OPEN（重置冷却时间）
```

### Agent 场景的熔断触发条件

| 条件 | 阈值建议 |
|------|----------|
| 同一工具调用连续失败 | ≥ 3 次 |
| 同一类错误在 5 次迭代内重复出现 | ≥ 3 次 |
| Token 消耗速率异常（单次迭代 > 预期 3 倍） | 立即触发 |
| Build 连续失败 | ≥ 3 次 |

熔断后的行为：
1. **暂停循环**
2. **输出根因分析报告**：什么操作失败了、为什么、已经尝试了哪些方案
3. **等待人工决策**：继续 / 换方案 / 放弃

## 示例：修复编译错误的完整重试流程

```
Agent 修改了 src/auth.ts
   → npm run build → FAIL (TypeError: undefined is not a function)

[错误分类] 编译/构建错误 → 允许重试，最多 3 次

Attempt 1: 分析错误 → 发现调用了不存在的函数 → grep 找到正确函数名
           → 修复 → npm run build → FAIL (另一个类型错误)

Attempt 2: 分析新错误 → 发现类型导入缺失 → 添加 import
           → 修复 → npm run build → PASS

[熔断器] 未触发（仅 1 次连续失败）
```

对比：

```
Agent 修改了 /etc/hosts（需要 sudo）
   → Permission denied

[错误分类] 权限拒绝 → 不允许重试 → 立即停止

[熔断器] 不触发（不是"连续失败"，而是"不可恢复错误"）
Agent 应立即向用户报告：需要什么权限、为什么需要、以及用户如何手动执行。
```

## 设计检查清单

- [ ] 所有可能的错误类型都有明确的分类（可恢复 vs 不可恢复）
- [ ] 指数退避有基数、上限和随机抖动
- [ ] 熔断器有明确的触发条件和冷却时间
- [ ] 熔断后的输出包含根因分析和已尝试方案
- [ ] 不可恢复错误立即停止，不做无意义重试

## 练习

为一个"调用 GitHub API 创建 PR"的 Agent 设计错误处理策略：

1. 列出至少 5 种可能发生的错误
2. 为每种错误分类并给出重试策略
3. 什么情况下需要设置熔断器？
4. 熔断后 Agent 应该输出什么给用户？

## 排错

| 症状 | 可能原因 | 修复 |
|------|----------|------|
| Agent 对同一个权限错误反复重试 | 错误分类缺失"不可恢复"类别 | 增加 PermissionDenied → 0 次重试 |
| 熔断器太敏感，正常波动就触发 | 连续失败阈值太低 | 调高阈值到 5 次 |
| 熔断器太迟钝，浪费了大量 Token 才触发 | 阈值太高或无 Token 监控 | 降低阈值到 3 次或增加 Token 速率检查 |
| 冷却后重试仍然失败 | 根因没有修复（比如 API key 已过期） | 熔断后输出应检查根因是否可自动修复 |

## 下一步

重试和熔断处理的是"单次操作失败"——接下来看[多源反馈](./multi-source-feedback)，理解如何从多个通道获取信号，让 Agent 更早发现错误。
